# 生产环境配置文件
# 用于阿里云服务器部署

# API服务器地址（可选，如果不设置会自动使用当前域名的3001端口）
# VITE_API_BASE_URL=http://8.134.236.143:3001/api

# 如果后端和前端在同一台服务器上，可以使用相对路径
# VITE_API_BASE_URL=/api

# 或者使用当前域名的3001端口（默认行为）
# VITE_API_BASE_URL=http://your-domain:3001/api